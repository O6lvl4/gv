name: release

on:
  push:
    tags:
      - "v*.*.*"
      - "v*.*.*-*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to (re)build (e.g. v0.1.0)"
        required: true

env:
  CARGO_TERM_COLOR: always
  CARGO_PROFILE_RELEASE_LTO: "fat"

jobs:
  build:
    name: build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-14
            cross: false
          - target: x86_64-apple-darwin
            # macos-13 (intel) was deprecated; cross-compile from arm64 instead.
            # Apple's clang/SDK on macos-14 supports x86_64 darwin natively.
            runner: macos-14
            cross: false
          - target: x86_64-unknown-linux-musl
            runner: ubuntu-latest
            cross: false
            apt: musl-tools
          - target: aarch64-unknown-linux-musl
            runner: ubuntu-latest
            cross: true
    steps:
      - uses: actions/checkout@v4

      - name: install cross compile prerequisites (linux x86_64-musl)
        if: matrix.apt
        run: sudo apt-get update && sudo apt-get install -y ${{ matrix.apt }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: build (native)
        if: ${{ !matrix.cross }}
        run: cargo build --release --locked --target ${{ matrix.target }} --bin gv --bin gv-shim

      - name: build (cross)
        if: matrix.cross
        run: cross build --release --locked --target ${{ matrix.target }} --bin gv --bin gv-shim

      - name: stage artifacts
        shell: bash
        run: |
          set -euo pipefail
          tag="${GITHUB_REF_NAME:-${{ github.event.inputs.tag }}}"
          stage="gv-${tag}-${{ matrix.target }}"
          mkdir -p "dist/${stage}"
          cp "target/${{ matrix.target }}/release/gv" "dist/${stage}/gv"
          cp "target/${{ matrix.target }}/release/gv-shim" "dist/${stage}/gv-shim"
          cp README.md LICENSE "dist/${stage}/" 2>/dev/null || true
          cd dist
          tar -czf "${stage}.tar.gz" "${stage}"
          shasum -a 256 "${stage}.tar.gz" > "${stage}.tar.gz.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: gv-${{ matrix.target }}
          path: dist/*.tar.gz*
          retention-days: 7

  release:
    name: publish release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: list artifacts
        run: ls -la dist/

      - name: combined checksums
        run: cd dist && shasum -a 256 *.tar.gz > SHA256SUMS

      - name: publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/SHA256SUMS

  bump-tap:
    name: bump homebrew tap
    needs: release
    runs-on: ubuntu-latest
    # Only bump on stable tags (skip rc/beta/alpha pre-releases that contain `-`).
    if: ${{ !contains(github.event.inputs.tag || github.ref_name, '-') }}
    steps:
      - name: checkout gv (for the formula template)
        uses: actions/checkout@v4

      - name: fetch SHA256SUMS from the just-published release
        env:
          TAG: ${{ github.event.inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          curl -fsSL "https://github.com/O6lvl4/gv/releases/download/${TAG}/SHA256SUMS" -o SHA256SUMS
          cat SHA256SUMS

      - name: render Formula/gv.rb
        env:
          TAG: ${{ github.event.inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          VERSION="${TAG#v}"
          sha_for() {
            awk -v f="gv-v${VERSION}-$1.tar.gz" '$2 == f { print $1 }' SHA256SUMS
          }
          SHA_DARWIN_ARM64=$(sha_for aarch64-apple-darwin)
          SHA_DARWIN_X86_64=$(sha_for x86_64-apple-darwin)
          SHA_LINUX_ARM64=$(sha_for aarch64-unknown-linux-musl)
          SHA_LINUX_X86_64=$(sha_for x86_64-unknown-linux-musl)
          for s in "$SHA_DARWIN_ARM64" "$SHA_DARWIN_X86_64" "$SHA_LINUX_ARM64" "$SHA_LINUX_X86_64"; do
            [ -n "$s" ] || { echo "missing sha for one or more targets" >&2; exit 1; }
          done
          mkdir -p out/Formula
          sed \
            -e "s|{{VERSION}}|${VERSION}|g" \
            -e "s|{{SHA_DARWIN_ARM64}}|${SHA_DARWIN_ARM64}|g" \
            -e "s|{{SHA_DARWIN_X86_64}}|${SHA_DARWIN_X86_64}|g" \
            -e "s|{{SHA_LINUX_ARM64}}|${SHA_LINUX_ARM64}|g" \
            -e "s|{{SHA_LINUX_X86_64}}|${SHA_LINUX_X86_64}|g" \
            packaging/homebrew/gv.rb.template > out/Formula/gv.rb
          echo "--- rendered formula ---"
          cat out/Formula/gv.rb

      - name: checkout homebrew-tap
        uses: actions/checkout@v4
        with:
          repository: O6lvl4/homebrew-tap
          ssh-key: ${{ secrets.TAP_DEPLOY_KEY }}
          path: tap

      - name: commit & push
        env:
          TAG: ${{ github.event.inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          VERSION="${TAG#v}"
          cp out/Formula/gv.rb tap/Formula/gv.rb
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/gv.rb
          if git diff --cached --quiet; then
            echo "tap formula already at v${VERSION}; nothing to commit"
            exit 0
          fi
          git commit -m "gv: bump to v${VERSION}"
          git push origin HEAD:main
